{
  "framework": null,
  "installCommand": "cd backend && npm install && cd ../frontend && npm install",
  "buildCommand": "cd frontend && npm run build && node scripts/copy-to-vercel-public.mjs",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/config.js", "destination": "/api" },
    { "source": "/legacy/(.*)", "destination": "/api" },
    { "source": "/((?!api/|legacy/).*)", "destination": "/index.html" }
  ]
}
