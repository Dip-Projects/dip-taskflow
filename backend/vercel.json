{
  "installCommand": "npm install && cd ../frontend && npm install",
  "buildCommand": "cd ../frontend && npm run build && test -f public/index.html",
  "functions": {
    "api/index.js": {
      "includeFiles": "{public/**,legacy/**,routes/**,middleware/**,lib/**}"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
